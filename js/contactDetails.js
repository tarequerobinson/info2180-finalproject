// To code  function for writing to table and for getting username 

function getUserbyID(id){

}

document.getElementById("comment").addEventListener(onclick, function clearText() {

    if (textarea.value === "Enter details here") {
        textarea.value = "";
    }
});

document.getElementById("comment").addEventListener(onclick, function restoreText() {
    
    if (textarea.value === "") {
        textarea.value = "Enter details here";
    }
});